node_modules
.next
out
.env
.env.local
.env*.local
data/*.jsonl
reference/*.xlsx
reference/*.xls
reference/*.csv
*.log
.DS_Store
.vercel
next-env.d.ts

.vercel
.env*

# raw upload folder — never commit (customer data + internal docs)
reference/

# build cache
tsconfig.tsbuildinfo

# eval scratch — regenerable from scripts (build-pilot / gen-video / ab-eval)
data/pilot/
